/**
 * Generate beautiful API docs directly from Express routes.
 * @example apidocsmith.scan(app)
 */
export function apidocsmith(): { ok: true; package: string } {
  return { ok: true, package: "apidocsmith" };
}
