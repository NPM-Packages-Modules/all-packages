/**
 * Generate advanced MongoDB queries from simple configs.
 * @example querygenie(Product).search(req.query)
 */
export function querygenie(): { ok: true; package: string } {
  return { ok: true, package: "querygenie" };
}
