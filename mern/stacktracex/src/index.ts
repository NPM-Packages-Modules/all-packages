/**
 * Visual distributed error tracing for MERN microservices.
 * @example stacktracex.track(app)
 */
export function stacktracex(): { ok: true; package: string } {
  return { ok: true, package: "stacktracex" };
}
